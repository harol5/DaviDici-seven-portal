export type Option = {
    code: string;
    imgUrl: string;
    title: string;
    validSkus: string[];
    isDisabled: boolean;
};

export type finish = {
    type: string;
    finish: string;
    url: string;
};

export type sinkPosition = {
    name: string;
    url: string;
};

export type model = {
    name: string;
    url: string;
};
